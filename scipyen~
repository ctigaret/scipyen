#! /bin/sh 

scipyexec=`realpath $0`
scipyendir=`dirname $scipyexec`
scipyenvdir=`dirname $scipyendir`

if [ -z $VIRTUAL_ENV ]; then
    source $scipyenvdir/bin/activate
fi

if [ -z $BROWSER ]; then
    source $scipyenvdir/bin/browser
fi
python3 $scipyendir/scipyen.py &
